{{ form_start(form) }}
<div class="row">
    <div class="form-group">
        <div class="">
            {{ form_row(form.type) }}
        </div>
    </div>
</div>
<div class="row">
    <div class="form-group">
        <div class="">
            {{ form_row(form.dateVisite) }}
        </div>
    </div>
</div>
<div class="row">
    <div class="form-group">
        <div class="">
            {{ form_row(form.nombreBillets) }}
        </div>
    </div>
</div>

<button class="btn btn-primary btn-xl">Je réserve !</button>


{{ form_end(form) }}